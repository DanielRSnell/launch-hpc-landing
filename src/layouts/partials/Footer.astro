---
import Logo from "@/components/Logo.astro";
import config from "@/config/config.json";
import menu from "@/config/menu.json";

const { footer, company, product, resources, services } = menu;

// Function to replace {year} this from string to year
function replaceYear(text: string) {
  const year = new Date().getFullYear();

  return text.replace("{year}", year.toString());
}
---

<footer class="py-10 md:py-16 xl:py-24">
  <div class="container" data-aos="fade-up-sm">
    <div class="row gy-6 justify-between lg:g-4">
      <div class="col-12 pb-7 md:pb-14">
        <Logo footerLogo={true} />
      </div>
      <div class="col-12 pt-5 md:pt-20">
        <div
          class="flex flex-col md:flex-row justify-between border-t border-border pt-10 pb-5">
          {
            config.params.copyright && (
              <div>
                <p class="text-sm text-white/60">
                  {replaceYear(config.params.copyright)}
                </p>
              </div>
            )
          }
          {
            (
              <div>
                <ul class="flex md:justify-end gap-4">
                  {footer?.map((item) => (
                    <li>
                      <a
                        class="text-sm text-white/60 transition hover:text-secondary"
                        href={item.url}>
                        {item.name}
                      </a>
                    </li>
                  ))}
                </ul>
              </div>
            )
          }
        </div>
      </div>
    </div>
  </div>
</footer>
